spring:
  application:
    name: APIGateway
  cloud:
    gateway:
      routes:
        - id: UserAndClassManagementService
          uri: lb://UserAndClassManagementService
          predicates:
            - Path=/api/v1/user/**
            - Path=/api/v1/class/**
          filters:
            - SetPath=/

        - id: AuthService
          uri: lb://AuthService
          predicates:
            - Path=/api/v1/auth/**

        - id: DiscoveryServer
          uri: http://localhost:8761
          predicates:
            - Path=/eureka/web
          filters:
            - SetPath=/

        - id: DiscoveryServer-static
          uri: http://localhost:8761
          predicates:
            - Path=/eureka/**

eureka:
  client:
    service-url:
      defaultZone: http://eureka:password@localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
